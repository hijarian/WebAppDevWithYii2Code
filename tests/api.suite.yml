class_name: ApiTester
modules:
    enabled:
      - ApiHelper
      - PhpBrowser
      - REST
      - Db
    config:
        PhpBrowser:
            url: 'http://localhost'
        REST:
            url: 'http://localhost'
        Db:
            dsn: 'mysql:host=localhost;dbname=crmapp'
            user: 'root'
            password: 'mysqlroot'
            dump: tests/_data/dump.sql

